import React from 'react';
import './App.css';

interface AppProps {}

function App({}: AppProps) {
  // Return the App component.
  return (
    <div className="App-header container">
      <h1>This is a Snowpack React Boilerplate.</h1>
    </div>
  );
}

export default App;
